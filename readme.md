It is just an sample library in Go language by using GIT.

echo "# go-common" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/xs2tarunkukreja/go-common.git
git push -u origin master